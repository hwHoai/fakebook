import { createContext } from "react";

export const AuthProvider = createContext(undefined);
export const UserInfoProvider = createContext(undefined);
export const GolbalLoadingProvider = createContext(undefined);
